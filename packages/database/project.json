{
  "name": "database",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/database/src",
  "tags": ["scope:database", "type:package"],
  "targets": {
    "migrate": {
      "command": "tsx --tsconfig tsconfig.base.json packages/database/src/apply-schema.ts"
    },
    "typecheck": {
      "command": "tsc -p packages/database/tsconfig.json --noEmit"
    }
  }
}
